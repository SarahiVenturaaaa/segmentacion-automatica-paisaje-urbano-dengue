#Paqueterias y versiones correspondientes para ejecutar de manera correcta los notebooks

numpy==2.0.2
pandas==2.2.3
matplotlib==3.9.4
seaborn==0.13.2

opencv-contrib-python-headless==4.11.0.86
pillow==11.2.1
scikit-image==0.24.0
tifffile==2024.8.30
imageio==2.37.0

scipy==1.13.1
scikit-learn==1.6.1
joblib==1.5.0
threadpoolctl==3.6.0

plotly==6.1.0
tqdm==4.67.1

umap-learn==0.5.7
pynndescent==0.5.13
numba==0.60.0
llvmlite==0.43.0

# Para el caso del modelo de redes neuronales, es necesario contar con las librerías
# `torch`, `torchvision` y `torchaudio`. Sin embargo, la instalación de PyTorch
# depende del sistema operativo y del tipo de hardware disponible, por ejemplo,
# CPU o GPU. Por ello, se recomienda instalarlo siguiendo el comando correspondiente
# indicado en la documentación oficial de PyTorch.
